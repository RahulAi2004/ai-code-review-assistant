# Python dependencies for the Vercel serverless function (api/index.py).
# (uvicorn and pytest are omitted here — Vercel provides the ASGI server, and
#  tests are not run in the serverless bundle. The full set is in server/requirements.txt.)
fastapi==0.115.6
google-generativeai==0.8.3
pymongo==4.10.1
mongomock==4.3.0
PyJWT==2.10.1
bcrypt==4.2.1
httpx==0.28.1
python-dotenv==1.0.1
